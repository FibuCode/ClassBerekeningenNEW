namespace BerekeningBfactorNamespace{
    class BerekeningBfactor {

        /*this class calculates the standard deviation from a dataset
        I plan on injecting input data into a list (an array of dynamic size) of doubles
        then if I can calculate the average and the sum number of data points
        I should be able to get the standard deviation*/

        //create dynamic list that updates based on input data
        public double [] metingen = {1,15,33,55,49};
        
        public double [] PopulateArr(){    
        for(double i = 0; i < 5; i++){
            Console.WriteLine(metingen[i]);
        }

    }    
        

        //write loop that prints all elements in list


        //calculate average from total elements in list
         


        

    }
}